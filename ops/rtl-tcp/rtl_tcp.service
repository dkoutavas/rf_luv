[Unit]
Description=RTL-SDR TCP streaming server
After=network.target
StartLimitIntervalSec=600
StartLimitBurst=20

[Service]
Type=simple
ExecStart=/usr/local/bin/rtl_tcp -a 0.0.0.0 -p 1234 -s 2048000
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
