# Classifier sanity check — 2026-04-18 17:25 local

Read-only pass covering pipeline liveness, the four cleanup fixes, known-signal
classification, distribution shape, scanner surface health, and listening_log
integrity. Nothing was changed.

## Status

**2 items flagged, both interpretive (no active regressions).**

1. Classifier_health's `atis_confidence_current` has sat at 0.4 since 12:40 today
   while the sanity-check-spec's ATIS target classifies at 0.8. Cause: the two
   pointers use different target frequencies (see Check 7 / Block 3 below).
2. Noise-floor check methodology doesn't match the recorded baseline — not a
   drift, a metric-definition mismatch.

Everything else green.

---

## Block 1 — pipeline liveness

**Check 1** — latest-row freshness, all four tables:

| Table | Last row | Age (s) |
|---|---|---:|
| scans | 2026-04-18 14:00:48.949 | 37 |
| peak_features | 2026-04-18 13:59:32 | 113 |
| signal_classifications | 2026-04-18 14:00:35 | 50 |
| classifier_health | 2026-04-18 14:00:47 | 38 |

All inside 2 min. Pipeline is live. ✓

*(times are UTC; local Athens is +3. "12:40 local" below = 09:40 UTC.)*

**Check 2** — classifier_health rows in last 2 h: **20**. Expected ~24, but
the timer was installed at ~12:32 UTC (~15:32 local), so the 2 h window starts
before the table existed. 20 rows in ~100 min of eligible time = exactly on
cadence. ✓

**Check 3** — sentinel metrics from latest classifier_health row:

| Metric | Current | Expected | Status |
|---|---:|---|---|
| confidence_precision_tail_count | 0 | 0 | ✓ |
| harmonic_flags_cross_allocation | 0 | 0 | ✓ |
| continuous_signals_with_bursts | 38 | ~43 (accepted baseline) | ✓ |
| confidence_distinct_values | 3 | 3–5 | ✓ |
| known_good_passing | 7/8 | 8/8 | ⚠ see Block 3 |

---

## Block 2 — cleanup fixes still holding

**Check 4 (fd5016e — precision):** `SELECT DISTINCT confidence` returns
`{0.4, 0.6, 0.8}`. Clean discrete set, no Float32 tails. ✓

**Check 5 (2b12a70 — harmonic):** `peak_features` total flagged = **0**.
Cross-allocation flagged = **0**. Filter fully holding. ✓

**Check 6 (43744b6 — burst):** Rows with `duty_24h > 0.85 AND burst_p50_s IS
NOT NULL AND burst_p50_s < 60` = **0**. Short-burst-on-continuous failure mode
not recurring. ✓

**Check 7 — ATIS stability last 20 classifier_health ticks:**

```
2026-04-18 14:00:47   0.4    ← current
…18 rows at 0.4…
2026-04-18 12:35:47   0.8    ← last 0.8 reading
2026-04-18 12:34:33   0.8
2026-04-18 12:32:37   0.8
```

Stepped 0.8 → 0.4 at ~12:40 UTC and held flat at 0.4 since. This is NOT a
flip-flop pattern (which would indicate burst-filter leakage) — it's a
persistent drop. **Cause identified in Block 3.** ⚠

---

## Block 3 — known-good reference table

All 9 sanity-check references pass against canonical target frequencies
(±150 kHz nearest-bin tolerance):

| Ref | Target MHz | Found MHz | class_id | conf | Expected | Status |
|---|---:|---:|---|---:|---|---|
| Athens ATIS | 136.125 | 136.034 | am_airband_atis | 0.8 | am_airband_atis ≥0.8 | ✓ |
| ATC Tower | 118.422 | 118.422 | am_airband_atc | 0.8 | am_airband_atc ≥0.6 | ✓ |
| ATC Approach | 118.522 | 118.522 | am_airband_atc | 0.8 | am_airband_atc ≥0.6 | ✓ |
| Gov VHF 150.49 | 150.490 | 150.490 | nfm_voice_repeater | 0.8 | nfm_voice_repeater ≥0.6 | ✓ |
| FM 99.59 | 99.590 | 99.590 | broadcast_fm | 0.8 | broadcast_fm ≥0.7 | ✓ |
| FM 105.734 | 105.734 | 105.734 | broadcast_fm | 0.8 | broadcast_fm ≥0.7 | ✓ |
| Marine near Ch16 | 156.834 | 156.834 | marine_vhf_channel | 0.6 | marine_vhf_channel ≥0.5 | ✓ |
| AIS | 161.975 | 161.878 | ais | 0.8 | ais ≥0.0 | ✓ |
| 164–168 cluster | 166.770 | 166.774 | nfm_voice_repeater | 0.8 | nfm_voice_repeater ≥0.6 | ✓ |

Sanity-check score: **9/9**.

**Why classifier_health says 7/8:** the health monitor's ATIS target is
`136_254_000` Hz (the scanner's full-sweep bin, 129 kHz off canonical
136.125). The sanity-check spec uses canonical `136_125_000`, which resolves
to the nearest-bin `136.034 MHz` at am_airband_atis @ 0.8.

Disambiguation query:

```
bin 136.034 (nearest to 136.125) → am_airband_atis @ 0.8  ✓ matches sanity spec
bin 136.254 (health monitor target) → am_airband_atc @ 0.4
```

So the 0.4 in Check 7 is real for bin 136.254, but bin 136.254 is classified
differently now — possibly a real drift, possibly because 136.254 is actually
closer to an airband ATC frequency than to ATIS. The canonical ATIS signal
lives at 136.034, which is 100% holding. Followup candidate: decide whether
the health monitor's ATIS pointer should be retargeted to 136_125_000.

---

## Block 4 — distribution shape

**Check 8** — current class distribution:

| class_id | count | session digest baseline | delta |
|---|---:|---:|---:|
| unknown_bursty | 403 | 398 | +5 |
| dvbt_mux | 210 | 121 | **+74%** (post-Issue-3 cleanup) |
| unknown_continuous | 170 | 216 | -21% |
| am_airband_atc | 117 | 116 | ±0 |
| nfm_voice_repeater | 78 | 68 | +15% |
| broadcast_fm | 67 | 66 | ±0 |
| am_airband_atis | 45 | 43 | ±0 |
| ais | 32 | 28 | +14% |
| marine_vhf_channel | 11 | 16 | -31% |
| tetra | 2 | 1 | +1 |

dvbt_mux jump is expected (Issue 3 cleared cross-allocation harmonic caps
and let those bins classify at 0.8 instead of being capped). Unknown_continuous
dropped correspondingly. Marine_vhf_channel -31% is noise-level (16 → 11 is a
small absolute change). ✓

**Check 9** — unknowns_ratio = **0.6724** vs baseline 0.66. Δ = +0.01. ✓

**Check 10** — bandwidth bucket distribution:

| Bucket | Count |
|---|---:|
| <300k (below floor) | 0 |
| =300k (narrow floor) | 203 |
| 300k–1M (mid) | 347 |
| 1M–5M (wide) | 0 |
| 5M+ (mux) | 0 |
| 999000000 (sentinel) | 570 |

Expected shape: 0 below floor (algorithm guarantees ≥300k), a cluster at floor
(narrow signals that find 3 dB drops at ±1 bin), a mid population (signals
spreading into ±2–3 bins), and a large sentinel count (DVB-T muxes + strong
wideband signals). **0 rows in 1M–5M or 5M+** confirms the observation: even
real DVB-T bins show either sub-1M (in-mux bin with local prominence) or
sentinel (edge bin with no 3 dB drop in ±5). No intermediate. ✓

---

## Block 5 — scanner surface health

**Check 11** — sweep cadence last hour (after filtering garbage lagInFrame
NULLs):

| Preset | Samples | Mean (s) | Stddev (s) | Min | Max |
|---|---:|---:|---:|---:|---:|
| full | 11 | 282.7 | 0.1 | 282.5 | 282.7 |
| airband | 59 | 60.3 | 0.2 | 60.2 | 60.9 |

Rock-solid. ±0.2 s on airband at 60 s cadence. ✓

**Check 12** — clip rate last hour: **11/71 sweeps clipped (15.5%)**,
avg max_clip_fraction = 0.0181, avg gain = **2.1 dB (at the floor)**.

Adaptive gain has stepped all the way down from initial 12 dB to the floor.
Even at floor, ~15% of sweeps show above-threshold clipping — unavoidable on
this antenna rig in Athens (strong DVB-T / FM / military VHF in view, stock
dipole on rooftop). This is an environment baseline rather than a regression.
Not actionable without a different antenna or a bandpass-filtered frontend.

**Check 13** — "noise floor" proxy = **-42.66 dBFS** across 43,849 samples,
computed as avg over last-hour full-sweep bins excluding 88–108 MHz, 136–137
MHz, 174–206 MHz. Recorded baseline is -48.9 dBFS (UHF Q10 per memory).

Δ = +6.2 dB **but methodologies don't match** — my query averages across all
non-excluded bands including the strong 146–174 MHz gov/mil VHF cluster which
pulls the mean up. The baseline was UHF-only Q10, not whole-band average.
Can't call drift without matched methodology. Followup: if a noise-floor
drift check is needed, recompute it using UHF-only Q10 and compare like-for-like.

---

## Block 6 — listening_log integrity

**Check 14** — 1 total row, 0 active overrides:

```
timestamp:         2026-04-10 13:18:27.444
freq_mhz:          166.77
class_id:          repeater_idle        (not a canonical signal_classes value)
confirmed_freq_hz: 0                    (→ no override fires)
heard / notes:     test_entry
```

A single test row from April 10 predating the step-1 schema migration. Not
triggering the override path (confirmed_freq_hz = 0). No operator confirmations
in production. Override feature remains only exercised by the step-3 smoke
test. Nothing to verify. ✓

---

## Followup candidates (not tonight)

1. **Health monitor's ATIS pointer mismatch.** classifier_health.py uses
   `136_254_000` as the ATIS target; sanity spec + canonical use `136_125_000`
   (which resolves to 136.034 MHz bin at 0.8). Decide whether to retarget the
   health monitor to the canonical value. One-line code change in
   `spectrum/classifier_health.py`. Would flip `known_good_passing` from 7/8
   to 8/8 and `atis_confidence_current` from 0.4 back to 0.8.
2. **Noise-floor metric methodology.** If this is going to be a tracked
   health metric, define it consistently: UHF-only Q10 per memory-baseline
   methodology. Add to classifier_health.py or leave as an ad-hoc query.
3. **Ambient clipping at floor gain.** 15% sweeps clipping with gain pegged
   at 2 dB is antenna/environment-limited. If ever bothersome, options are a
   bandpass filter (exclude the 88–108 FM band before the ADC) or moving the
   dipole. Not urgent.
4. **Stale test row in listening_log** (2026-04-10 "repeater_idle"). Safe
   to `ALTER TABLE ... DELETE` whenever — doesn't trigger the override
   because `confirmed_freq_hz = 0`. Hygiene only.

## Out of scope tonight — nothing fixed here.
